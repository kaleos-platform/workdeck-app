# 쿠팡 광고 리포트 매니저 MVP PRD v3 (통합본)

## 1. 제품 개요

- **목적**: 쿠팡 광고 리포트 Excel을 업로드·분석해 비효율 광고비를 절감하고 ROAS를 개선한다.
- **핵심 사용자**: 쿠팡 광고를 직접 운영하는 1인 셀러 또는 소규모 쇼핑몰 운영자.
- **MVP 가치**: 업로드 1회로 캠페인 성과 추이, 원시 데이터 탐색, 비효율 키워드 추출, 일자 메모 관리까지 수행한다.

---

## 2. 범위

### MVP 포함

| ID   | 기능명                                       |
| ---- | -------------------------------------------- |
| F001 | 광고 리포트 업로드 (.xlsx 파싱/정규화/저장)  |
| F002 | 시계열 성과 그래프                           |
| F003 | 비효율 키워드 분석 및 클립보드 복사          |
| F004 | 광고 데이터 테이블 (정렬/페이지네이션)       |
| F005 | 일자별 메모 CRUD                             |
| F006 | 날짜 범위 필터 (탭 공통)                     |
| F007 | 광고유형 필터 (탭 공통)                      |
| F008 | 계산 지표 엔진 (CTR/CVR/ROAS 기간 합산 계산) |
| F010 | 기본 인증 (회원가입/로그인/로그아웃)         |
| F011 | 워크스페이스 생성                            |

### MVP 제외

- 팀원 초대 및 협업
- 자동 스케줄 리포트 수집
- 쿠팡 광고 API 직접 연동
- 키워드 입찰가 추천
- 실시간 알림 시스템
- 프로필 상세 관리 (아바타, 자기소개 등)

---

## 3. 사용자 여정

```
1. 회원가입 페이지
   ↓ 이메일·비밀번호 입력 후 가입 완료

2. 워크스페이스 설정 페이지 (최초 1회)
   ↓ 쿠팡 사업자명 입력 후 생성

3. 대시보드 (전체 워크스페이스 요약)
   - 업로드 데이터 없음 → 리포트 업로드 CTA 표시
   ↓ 업로드 버튼 클릭

4. 파일 업로드 페이지
   - Excel(.xlsx) 파일 선택 또는 드래그앤드롭
   - 파싱 → DB 저장 → NVB 캠페인 목록 갱신
   ↓ 업로드 성공

5. 캠페인 상세 페이지 (NVB에서 캠페인 선택)
   - [대시보드 탭]    → 시계열 그래프 + 일자별 메모
   - [광고 데이터 탭] → 원시 데이터 테이블 (정렬·필터)
   - [키워드 분석 탭] → 비효율 키워드 목록 + 다중 선택 + 클립보드 복사
   ↓ 각 탭 상단의 공통 필터 조작

6. 날짜 범위·광고유형 필터
   - 모든 탭에 공통 적용, 필터 변경 시 해당 탭 데이터 즉시 갱신
```

---

## 4. 기능 명세 및 수락 기준

### 4.1 핵심 분석 기능

#### F001 광고 리포트 업로드

- **설명**: `.xlsx` 파일을 검증/파싱/정규화 후 `ReportUpload`, `AdRecord`를 저장한다.
- **수락 기준**:
  - 유효한 `.xlsx` 업로드 시 `inserted + updated > 0`을 반환해야 한다.
  - 유효하지 않은 확장자 업로드 시 `400 INVALID_FILE`을 반환해야 한다.
  - 동일 데이터 재업로드 시 업서트 키 기준으로 중복 행이 증가하지 않아야 한다.
  - 업로드 진행 중 프로그레스 바로 상태를 표시해야 한다.
  - 파싱 시 직접 주문수(`orders1d`) 및 직접 전환매출액(`revenue1d`) 컬럼을 우선 추출 대상으로 사용한다.

#### F002 시계열 성과 그래프

- **설명**: 날짜별 **총 광고비**, **평균 ROAS**, **클릭율(CTR)**, **전환율(CVR)** 추이를 Recharts **라인 차트**로 표시한다. 기간 합산 계산 지표(F008)를 기반으로 렌더링한다.
- **기본 기간**: 오늘 기준 14일 전 ~ 오늘
- **수락 기준**:
  - 기간 필터 변경 시 차트가 즉시 갱신되어야 한다.
  - 데이터가 없으면 빈 상태 UI와 업로드 CTA를 표시해야 한다.
  - 표시 지표: 총광고비·평균ROAS·클릭율(CTR)·전환율(CVR) 4개 라인 그래프로 렌더링되어야 한다.
  - 퀵 기간 설정(오늘/7일/14일/30일/90일/180일/이번달/지난달) 버튼이 표시되고 선택 시 날짜 범위가 갱신되어야 한다.
  - 차트 영역 클릭 시 해당 날짜 메모 입력창이 표시되어야 한다.
  - 저장된 메모가 있는 날짜는 차트에 아이콘으로 표시되어야 하며, 클릭 시 메모 내용을 확인할 수 있어야 한다.

#### F003 비효율 키워드 분석

- **설명**: `adCost > 0 AND orders1d = 0 AND keyword NOT IN ('-', '', null)` 조건의 키워드를 조회·복사한다.
- **수락 기준**:
  - 조건 일치 데이터가 있으면 테이블에 표시되어야 한다.
  - 체크박스 다중 선택(전체 선택 포함)이 가능해야 한다.
  - 선택 복사 시 쉼표(`,`) 구분 문자열이 클립보드에 저장되어야 한다.
  - 테이블 컬럼 구성: 키워드, 광고비, CTR, CVR, ROAS
  - 각 컬럼 헤더 클릭 시 오름/내림 정렬 기능이 작동해야 한다.

#### F004 광고 데이터 테이블

- **설명**: 캠페인 원시 데이터를 정렬/페이지네이션으로 탐색한다.
- **수락 기준**:
  - `sortBy`, `sortOrder` 변경 시 일관된 결과를 반환해야 한다.
  - `pageSize`(10/25/50) 변경 시 응답 행 수가 반영되어야 한다.
  - 컬럼 표시·숨기기 토글이 작동해야 한다.
  - 광고 노출 지면(`placement`) 컬럼이 테이블에 표시되어야 한다.
  - 광고 상품명은 첫 번째 쉼표 앞 텍스트로 파싱 표시하고, 옵션명은 '구성'·'사이즈' 값을 '/' 구분하여 중복 제거 후 표시해야 한다.
  - CTR, CVR, ROAS 계산 지표 컬럼이 추가되어야 한다. (F008 기반 행 단위 계산)

#### F005 일자별 메모 CRUD

- **설명**: 캠페인 단위 날짜 메모를 생성/조회/수정/삭제한다.
- **고유키**: `(workspaceId, campaignId, date)`
- **수락 기준**:
  - 동일 날짜 재저장 시 upsert로 1건만 유지해야 한다.
  - 삭제 후 재조회 시 해당 메모가 노출되지 않아야 한다.
  - 대시보드 탭 그래프와 연동되어 날짜별 메모를 확인할 수 있어야 한다.
  - 메모 목록은 선택 기간 내에 있는 메모만 표시해야 한다.
  - 메모 영역 우측 상단에 '메모 추가' 버튼이 있으며, 클릭 시 날짜·내용을 직접 입력하는 폼이 표시되어야 한다.

### 4.2 공통 및 지원 기능

#### F006 날짜 범위 필터

- **설명**: 캠페인 상세 3개 탭에 공통 적용하는 날짜 범위 필터.
- **상태 정책**: URL Query를 단일 소스로 사용한다.
- **수락 기준**:
  - 탭 이동 시 필터 값이 유지되어야 한다.
  - 필터 변경 시 활성 탭 데이터가 즉시 재조회되어야 한다.
  - 기본 기간은 오늘 기준 14일 전 ~ 오늘로 설정되어야 한다.
  - 퀵 기간 설정 버튼(오늘/7일/14일/30일/90일/180일/이번달/지난달)이 제공되어야 한다.

#### F007 광고유형 필터

- **설명**: 캠페인 상세 3개 탭에 공통 적용하는 광고유형 드롭다운 필터.
- **상태 정책**: URL Query를 단일 소스로 사용한다.
- **수락 기준**:
  - 탭 이동 시 필터 값이 유지되어야 한다.
  - 필터 변경 시 활성 탭 데이터가 즉시 재조회되어야 한다.

#### F010 기본 인증

- **설명**: Supabase Auth 기반 이메일/비밀번호 회원가입·로그인·로그아웃.
- **수락 기준**:
  - 유효한 이메일·비밀번호로 회원가입 후 워크스페이스 설정 페이지로 이동해야 한다.
  - 로그인 후 대시보드로 이동해야 한다.
  - 미인증 상태로 대시보드 접근 시 로그인 페이지로 리다이렉트해야 한다.

#### F011 워크스페이스 생성

- **설명**: 최초 가입 시 쿠팡 사업자명 기반 워크스페이스 1개 생성.
- **수락 기준**:
  - 사업자명 1자 이상 입력 시 워크스페이스가 생성되어야 한다.
  - 워크스페이스 미생성 사용자가 대시보드 접근 시 워크스페이스 설정 페이지로 리다이렉트해야 한다.
  - 1인 1워크스페이스 제한을 적용해야 한다.

### 4.3 계산 지표 기능

#### F008 계산 지표 엔진

- **설명**: CTR·CVR·ROAS를 기간 합산 기반으로 계산하는 공통 로직.
- **계산식**:
  - 클릭율(CTR) = (총 클릭수 / 총 노출수) × 100
  - 전환율(CVR) = (총 주문수 / 총 클릭수) × 100
  - ROAS = (총 전환매출액 / 총 광고비) × 100
- **소수점 정책**: 소수점 1자리 반올림 (예: 12.3%, 450.5%)
- **기준**: 주문수·전환매출액은 직접(1일) 기준만 사용 (`orders1d`, `revenue1d`)
- **수락 기준**:
  - 기간 내 합산 후 계산 결과가 소수점 1자리 반올림으로 반환되어야 한다.
  - 분모가 0인 경우 `null` 또는 `-`로 표시되어야 한다.
  - CTR·CVR·ROAS는 프론트엔드와 API 계층 모두에서 일관되게 계산되어야 한다.

---

## 5. 페이지별 상세 기능

### 회원가입 페이지

> **구현 기능:** `F010` | **인증:** 비로그인 전용

| 항목          | 내용                                                                                                              |
| ------------- | ----------------------------------------------------------------------------------------------------------------- |
| **역할**      | 신규 사용자 계정 생성 전용 페이지                                                                                 |
| **진입 경로** | 랜딩 CTA 버튼 클릭, 로그인 페이지의 "회원가입" 링크 클릭                                                          |
| **주요 기능** | 이메일·비밀번호 입력 폼, Zod 스키마 기반 실시간 유효성 검사, Supabase Auth 회원가입 처리, 로그인 페이지 이동 링크 |
| **다음 이동** | 성공 → 워크스페이스 설정 페이지 (자동 리디렉션), 실패 → 에러 메시지 표시                                          |

---

### 로그인 페이지

> **구현 기능:** `F010` | **인증:** 비로그인 전용

| 항목          | 내용                                                                                                       |
| ------------- | ---------------------------------------------------------------------------------------------------------- |
| **역할**      | 기존 사용자 인증 전용 페이지                                                                               |
| **진입 경로** | 랜딩 페이지, 미인증 상태로 대시보드 접근 시 자동 리디렉션                                                  |
| **주요 기능** | 이메일·비밀번호 입력 폼, Zod 스키마 기반 유효성 검사, Supabase Auth 로그인 처리, 회원가입 페이지 이동 링크 |
| **다음 이동** | 성공 → 대시보드, 실패 → 에러 메시지 표시                                                                   |

---

### 워크스페이스 설정 페이지

> **구현 기능:** `F011` | **인증:** 로그인 필수, 최초 1회

| 항목          | 내용                                                                                                |
| ------------- | --------------------------------------------------------------------------------------------------- |
| **역할**      | 쿠팡 사업자 기준 워크스페이스 최초 생성 페이지                                                      |
| **진입 경로** | 회원가입 완료 후 자동 리디렉션 (워크스페이스 미생성 사용자만 접근)                                  |
| **주요 기능** | 쿠팡 사업자명 텍스트 입력 폼, Zod 스키마 기반 유효성 검사 (최소 1자 이상), Workspace DB 레코드 생성 |
| **다음 이동** | 성공 → 대시보드, 실패 → 에러 메시지 표시                                                            |

---

### 대시보드

> **구현 기능:** `F001` 진입점 | **인증:** 로그인 필수

| 항목          | 내용                                                                                                                                                              |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **역할**      | 전체 워크스페이스 성과 요약 및 서비스 허브 역할                                                                                                                   |
| **진입 경로** | 로그인 후 기본 진입, NVB 홈(대시보드) 메뉴 클릭                                                                                                                   |
| **주요 기능** | 전체 광고비·ROAS·클릭수 요약 KPI 카드, 업로드 파일 이력 목록 (파일명·업로드 일시·데이터 기간), 데이터 없을 시 리포트 업로드 CTA 배너 표시, **리포트 업로드** 버튼 |
| **다음 이동** | 업로드 버튼 클릭 → 파일 업로드 페이지, NVB 캠페인 클릭 → 캠페인 상세 페이지                                                                                       |

---

### 파일 업로드 페이지

> **구현 기능:** `F001` | **인증:** 로그인 필수

| 항목          | 내용                                                                                                                                                                                                                         |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **역할**      | 쿠팡 광고 리포트 Excel 파일 파싱 및 DB 저장 전용 페이지                                                                                                                                                                      |
| **진입 경로** | 대시보드의 "리포트 업로드" 버튼 클릭, NVB "리포트 업로드" 메뉴 클릭                                                                                                                                                          |
| **주요 기능** | 드래그앤드롭 영역 및 파일 선택 버튼, .xlsx 확장자 유효성 검사, SheetJS(xlsx)로 Excel 파싱, 파싱 진행 상태 표시 (프로그레스 바), 업서트 키 기준 중복 데이터 처리, AdRecord 및 ReportUpload DB 저장, NVB 캠페인 목록 자동 갱신 |
| **다음 이동** | 성공 → 캠페인 상세 페이지 (첫 번째 캠페인 자동 이동), 실패 → 에러 메시지 표시                                                                                                                                                |

---

### 캠페인 상세 페이지 - 대시보드 탭

> **구현 기능:** `F002`, `F005`, `F006`, `F007`, `F008` | **인증:** 로그인 필수

| 항목          | 내용                                                                                                                                                                                                                                                                                                                                                |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **역할**      | 선택 캠페인의 성과 트렌드 시각화 및 작업 이력 메모 관리                                                                                                                                                                                                                                                                                             |
| **진입 경로** | NVB에서 캠페인명 클릭 (기본 진입 탭), 캠페인 상세에서 "대시보드" 탭 클릭                                                                                                                                                                                                                                                                            |
| **주요 기능** | 퀵 기간 설정 버튼 (오늘/7일/14일/30일/90일/180일/이번달/지난달·F006), 광고유형 드롭다운 필터 (F007·탭 공통), Recharts 라인 차트 (총광고비·평균ROAS·CTR·CVR·F002·F008), 차트 클릭 → 해당 날짜 메모 입력창 표시 (F005), 저장 메모 차트 아이콘 표시 및 클릭 확인 (F005), 메모 목록 (선택 기간 내 필터·F005), 메모 추가 버튼 (날짜+내용 직접 입력·F005) |
| **다음 이동** | 탭 클릭 → 광고 데이터 탭 또는 키워드 분석 탭                                                                                                                                                                                                                                                                                                        |

---

### 캠페인 상세 페이지 - 광고 데이터 탭

> **구현 기능:** `F004`, `F006`, `F007`, `F008` | **인증:** 로그인 필수

| 항목          | 내용                                                                                                                                                                                                                                                                                  |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **역할**      | 선택 캠페인의 원시 광고 데이터 탐색 및 비교                                                                                                                                                                                                                                           |
| **진입 경로** | 캠페인 상세 페이지에서 "광고 데이터" 탭 클릭                                                                                                                                                                                                                                          |
| **주요 기능** | 날짜 범위 피커 (F006·탭 공통), 광고유형 드롭다운 필터 (F007·탭 공통), 컬럼별 오름/내림차순 정렬 (F004), 페이지 크기 선택 (10/25/50행), 컬럼 표시·숨기기 토글, 광고 노출 지면 컬럼 표시, 상품명 파싱(첫 쉼표 앞)/옵션명 파싱('구성'/'사이즈' 기준), CTR·CVR·ROAS 계산 지표 컬럼 (F008) |
| **다음 이동** | 탭 클릭 → 대시보드 탭 또는 키워드 분석 탭                                                                                                                                                                                                                                             |

---

### 캠페인 상세 페이지 - 키워드 분석 탭

> **구현 기능:** `F003`, `F006`, `F007`, `F008` | **인증:** 로그인 필수

| 항목          | 내용                                                                                                                                                                                                                                                                                                |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **역할**      | 광고비 낭비 키워드(비효율 키워드) 발견 및 클립보드 복사                                                                                                                                                                                                                                             |
| **진입 경로** | 캠페인 상세 페이지에서 "키워드 분석" 탭 클릭                                                                                                                                                                                                                                                        |
| **주요 기능** | 광고유형 드롭다운 필터 (F007·탭 공통), 비효율 키워드 테이블 (adCost > 0 & orders1d = 0 조건 자동 필터링·F003), 키워드별 광고비·CTR·CVR·ROAS 컬럼 표시 (F008), 각 컬럼 헤더 클릭 시 오름/내림 정렬 기능, 체크박스 행 다중 선택 (전체 선택 포함), **선택 키워드 복사** 버튼 (쉼표 구분 클립보드 복사) |
| **다음 이동** | 탭 클릭 → 대시보드 탭 또는 광고 데이터 탭                                                                                                                                                                                                                                                           |

---

## 6. 메뉴 구조

```
📱 쿠팡 광고 리포트 매니저 - 사이드바(NVB)
├── 🏠 대시보드
│   └── 기능: 전체 워크스페이스 성과 요약, 업로드 이력, 리포트 업로드 CTA
├── 📤 리포트 업로드
│   └── 기능: F001 (Excel 파일 업로드·파싱·저장)
├── ──────────────
└── 📊 캠페인 목록
    ├── [캠페인명1] → 캠페인 상세 페이지
    ├── [캠페인명2] → 캠페인 상세 페이지
    └── ... (업로드된 Excel에서 파싱된 캠페인 자동 목록화)

📄 캠페인 상세 페이지 - 탭 구조
├── 📈 대시보드 탭
│   └── 기능: F002 (시계열 그래프), F005 (일자별 메모), F006 (날짜 범위 필터), F007 (광고유형 필터), F008 (계산 지표)
├── 📋 광고 데이터 탭
│   └── 기능: F004 (데이터 테이블), F006 (날짜 범위 필터), F007 (광고유형 필터), F008 (계산 지표)
└── 🔍 키워드 분석 탭
    └── 기능: F003 (비효율 키워드 분석), F006 (날짜 범위 필터), F007 (광고유형 필터), F008 (계산 지표)

👤 인증 메뉴 (비로그인 시)
├── 로그인 - F010
└── 회원가입 - F010

⚙️ 공통 메뉴 (로그인 후)
└── 🚪 로그아웃
```

---

## 7. 데이터 계약 (Excel → 정규화)

> **분석 기준 컬럼**: `adCost`, `impressions`, `clicks`, `orders1d`(직접 주문수 1일), `revenue1d`(직접 전환매출액 1일)을 주요 지표 계산 기준으로 사용한다. 14일 기준 컬럼은 참고용으로만 저장한다.

### 7.1 입력 컬럼 (핵심)

| 쿠팡 Excel 컬럼명   | DB 필드        |
| ------------------- | -------------- |
| 날짜                | `date`         |
| 광고유형            | `adType`       |
| 캠페인 ID           | `campaignId`   |
| 캠페인명            | `campaignName` |
| 광고그룹            | `adGroup`      |
| 광고집행 상품명     | `productName`  |
| 광고집행 옵션ID     | `optionId`     |
| 광고 노출 지면      | `placement`    |
| 키워드              | `keyword`      |
| 노출수              | `impressions`  |
| 클릭수              | `clicks`       |
| 광고비              | `adCost`       |
| 클릭률              | `ctr`          |
| 총 주문수(1일)      | `orders1d`     |
| 총 전환매출액(1일)  | `revenue1d`    |
| 총광고수익률(1일)   | `roas1d`       |
| 총 주문수(14일)     | `orders14d`    |
| 총 전환매출액(14일) | `revenue14d`   |
| 총광고수익률(14일)  | `roas14d`      |

### 7.2 정규화 규칙

- **날짜**: `YYYYMMDD` 또는 날짜 문자열을 `Asia/Seoul` 기준 날짜로 변환한다.
- **퍼센트**:
  - `"12.34%"` → `0.1234`
  - `"5.6689E-4"` → `0.00056689`
- **결측값**:
  - `"-"` 또는 빈 문자열은 컬럼 정책에 따라 `null` 또는 `0` 처리한다.
  - `keyword`의 `"-"`는 `null`로 정규화한다.
  - 수치 컬럼의 `"-"` 또는 빈 문자열은 `0`으로 처리한다.

### 7.3 업서트 키 정책 (최종)

- **복합 키**: `workspaceId + date + campaignId + adType + keyword + adGroup + optionId`
- 충돌 시 최신 업로드 값으로 갱신한다.
- "중복 날짜 병합" 정책은 사용하지 않는다.

---

## 8. 데이터 모델

### Workspace (워크스페이스)

| 필드        | 설명                    | 타입     |
| ----------- | ----------------------- | -------- |
| `id`        | 고유 식별자             | UUID     |
| `name`      | 쿠팡 사업자명           | String   |
| `ownerId`   | 소유 사용자 (→ User.id) | String   |
| `createdAt` | 생성 일시               | DateTime |

### ReportUpload (업로드 이력)

| 필드          | 설명                               | 타입     |
| ------------- | ---------------------------------- | -------- |
| `id`          | 고유 식별자                        | UUID     |
| `workspaceId` | 소속 워크스페이스 (→ Workspace.id) | String   |
| `fileName`    | 원본 파일명                        | String   |
| `uploadedAt`  | 업로드 일시                        | DateTime |
| `periodStart` | 데이터 시작일                      | DateTime |
| `periodEnd`   | 데이터 종료일                      | DateTime |

### AdRecord (광고 데이터 행)

| 필드           | 설명                                 | 타입     |
| -------------- | ------------------------------------ | -------- |
| `id`           | 고유 식별자                          | UUID     |
| `workspaceId`  | 소속 워크스페이스 (→ Workspace.id)   | String   |
| `reportId`     | 소속 업로드 이력 (→ ReportUpload.id) | String   |
| `date`         | 광고 날짜                            | DateTime |
| `adType`       | 광고유형                             | String   |
| `campaignId`   | 캠페인 ID                            | String   |
| `campaignName` | 캠페인명                             | String   |
| `adGroup`      | 광고그룹                             | String?  |
| `placement`    | 광고 노출 지면                       | String?  |
| `productName`  | 광고집행 상품명                      | String?  |
| `optionId`     | 광고집행 옵션ID                      | String?  |
| `keyword`      | 키워드                               | String?  |
| `impressions`  | 노출수                               | Int      |
| `clicks`       | 클릭수                               | Int      |
| `adCost`       | 광고비                               | Decimal  |
| `ctr`          | 클릭률                               | Decimal  |
| `orders1d`     | 총 주문수 (1일)                      | Int      |
| `revenue1d`    | 총 전환매출액 (1일)                  | Decimal  |
| `roas1d`       | 총 광고수익률 (1일)                  | Decimal  |
| `orders14d`    | 총 주문수 (14일)                     | Int      |
| `revenue14d`   | 총 전환매출액 (14일)                 | Decimal  |
| `roas14d`      | 총 광고수익률 (14일)                 | Decimal  |

### DailyMemo (일자별 메모)

| 필드          | 설명                               | 타입     |
| ------------- | ---------------------------------- | -------- |
| `id`          | 고유 식별자                        | UUID     |
| `workspaceId` | 소속 워크스페이스 (→ Workspace.id) | String   |
| `campaignId`  | 캠페인 ID                          | String   |
| `date`        | 메모 날짜                          | DateTime |
| `content`     | 메모 내용                          | String   |
| `createdAt`   | 생성 일시                          | DateTime |
| `updatedAt`   | 수정 일시                          | DateTime |

- **유니크 인덱스**: `(workspaceId, campaignId, date)`

---

## 9. API 계약

### `POST /api/reports/upload`

- **Request**: `multipart/form-data` (`file`: .xlsx)
- **Response**: `{ uploadId, inserted, updated, skipped, errors[] }`
- **Errors**: `400 INVALID_FILE`, `422 PARSE_ERROR`, `401 UNAUTHORIZED`, `500 INTERNAL_ERROR`

### `POST /api/workspace`

- **Request**: `{ name: string }`
- **Response**: `{ id, name, createdAt }`
- **Errors**: `400 INVALID_NAME`, `409 ALREADY_EXISTS`, `401 UNAUTHORIZED`

### `GET /api/campaigns`

- **Query**: `workspaceId`
- **Response**: `[{ campaignId, campaignName, adTypes[] }]`

### `GET /api/campaigns/:campaignId/metrics`

- **Query**: `from`, `to`, `adType=all|...`
- **Response**: `{ series: [{ date, adCost, impressions, clicks, orders1d, revenue1d, ctr, cvr, roas }] }`
- **비고**: `ctr`, `cvr`, `roas`는 서버 사이드에서 기간 합산 계산 결과 (F008 적용)

### `GET /api/campaigns/:campaignId/records`

- **Query**: `from`, `to`, `adType`, `page`, `pageSize`, `sortBy`, `sortOrder`
- **Response**: `{ items: [AdRecord], page, pageSize, total }`

### `GET /api/campaigns/:campaignId/inefficient-keywords`

- **Query**: `from`, `to`, `adType`
- **Response**: `{ items: [{ keyword, adCost, ctr, cvr, roas }] }`
- **필터 조건**: `adCost > 0 AND orders1d = 0 AND keyword NOT IN ('-', '', null)`

### `POST /api/campaigns/:campaignId/memos`

- **Body**: `{ date: string, content: string }` (upsert)
- **Response**: `{ id, campaignId, date, content, updatedAt }`

### `DELETE /api/campaigns/:campaignId/memos`

- **Query**: `date`
- **Response**: `{ success: true }`

---

## 10. 예외 처리 및 실패 UX

| 상황          | 처리 방법                                                   |
| ------------- | ----------------------------------------------------------- |
| 확장자 오류   | `400 INVALID_FILE` + "xlsx 파일만 업로드 가능합니다" 메시지 |
| 파싱 오류     | `422 PARSE_ERROR` + 구체적인 행(row) 또는 컬럼 원인 메시지  |
| 권한 오류     | `401 UNAUTHORIZED` + 로그인 페이지 리다이렉트               |
| 데이터 없음   | 탭별 빈 상태 UI + 다음 행동 CTA (업로드/필터 초기화)        |
| 네트워크 오류 | 에러 토스트 + 재시도 액션 버튼 제공                         |

---

## 11. 비기능 요구사항

| 항목          | 기준                                             |
| ------------- | ------------------------------------------------ |
| **시간대**    | `Asia/Seoul` 기준                                |
| **성능**      | 50,000행 파일 기준 60초 내 처리 목표             |
| **보안**      | 모든 API: 세션 인증 + workspace 소유권 검증 필수 |
| **감사성**    | 업로드 이력(`ReportUpload`)을 반드시 기록        |
| **중복 방지** | 업서트 키 기준 idempotent 저장                   |

---

## 12. 테스트 시나리오

### 단위 테스트

- 날짜/퍼센트/과학적표기법/결측값 정규화 함수
- 업서트 키 생성 및 충돌 처리 로직
- CTR·CVR·ROAS 계산 함수 (분모 0 케이스 포함)

### 통합 테스트

- 동일 파일 재업로드 idempotency 검증
- 필터 조건별 조회 정확성 (`from`, `to`, `adType` 조합)
- 비효율 키워드 필터 조건 (`adCost > 0`, `orders1d = 0`, `keyword != null`)
- metrics API 응답의 `ctr`, `cvr`, `roas` 계산값 정합성 검증

### E2E 테스트

- 가입 → 워크스페이스 생성 → 업로드 → 캠페인 상세 진입 전체 플로우
- 필터 변경 시 3개 탭 동기 반영 확인
- 퀵 기간 설정 버튼 선택 시 날짜 범위 갱신 확인
- 비효율 키워드 선택 복사 (클립보드 검증)
- 메모 CRUD 후 재조회 일치 확인
- 차트 날짜 클릭 → 메모 입력창 표시 확인

---

## 13. 기술 스택

### 프론트엔드

| 항목            | 기술                         |
| --------------- | ---------------------------- |
| 프레임워크      | Next.js 16 (App Router, RSC) |
| 언어            | TypeScript 5                 |
| UI 라이브러리   | React 19                     |
| 스타일링        | Tailwind CSS v4              |
| UI 컴포넌트     | shadcn/ui (new-york 스타일)  |
| 아이콘          | Lucide React                 |
| 차트            | Recharts                     |
| 폼 관리         | React Hook Form 7.x          |
| 스키마 검증     | Zod                          |
| 클라이언트 상태 | Zustand                      |

### 백엔드 & 데이터베이스

| 항목         | 기술                                |
| ------------ | ----------------------------------- |
| 인증         | Supabase Auth (SSR 방식)            |
| ORM          | Prisma 7                            |
| 데이터베이스 | PostgreSQL (Supabase 포함)          |
| 파일 파싱    | xlsx (SheetJS) — `npm install xlsx` |

### 배포

| 항목        | 기술   |
| ----------- | ------ |
| 호스팅      | Vercel |
| 패키지 관리 | npm    |

### 환경 변수

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
DATABASE_URL
```

---

## 14. 기본 가정

- 사용자당 워크스페이스 1개 모델을 유지한다.
- 비효율 키워드 판단 주문 기준은 `orders1d`를 사용한다.
- CTR·CVR·ROAS 계산 시 `orders1d`, `revenue1d` (직접 1일 기준)만 사용하며, 14일 기준 컬럼은 참고용으로만 저장한다.
- 초기 버전은 한국어 UI만 제공한다.
- Prisma datasource URL은 `prisma.config.ts`에서 관리한다 (`schema.prisma`에 url 없음).
